---
phase: 260501-a3u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/reveal-on-scroll.tsx
  - src/components/landing/reveal-on-scroll.test.tsx
  - src/components/landing/schools-today.tsx
  - src/components/landing/audience-columns.tsx
  - src/components/landing/final-cta.tsx
autonomous: true
requirements:
  - QUICK-260501-a3u
must_haves:
  truths:
    - "Each post-hero section (SchoolsToday, AudienceColumns, FinalCta) fades and lifts into view as the user scrolls past ~85% viewport entry."
    - "Reveals fire exactly once — scrolling back up keeps elements in their final state."
    - "Under prefers-reduced-motion, all reveal targets render at the final state with no transform/opacity transition."
    - "SchoolsToday's three case-study cards stagger left-to-right (~80ms apart); AudienceColumns' three columns stagger ~100ms apart."
    - "FinalCta wraps as a single composed reveal with no inner stagger."
    - "scroll-choreography/**, paper-hero.tsx, feature-section.tsx, styles.css, and content/landing.ts are not modified."
  artifacts:
    - path: "src/components/landing/reveal-on-scroll.tsx"
      provides: "Reusable RevealOnScroll wrapper using motion/react useInView + useReducedMotion"
      exports: ["RevealOnScroll"]
      min_lines: 30
    - path: "src/components/landing/reveal-on-scroll.test.tsx"
      provides: "Render test covering reduced-motion final-state path + once-only contract"
      contains: "useReducedMotion"
    - path: "src/components/landing/schools-today.tsx"
      provides: "SchoolsToday wrapped: heading block + 3 cards with delay={i * 80}"
      contains: "RevealOnScroll"
    - path: "src/components/landing/audience-columns.tsx"
      provides: "AudienceColumns wrapped: 3 columns with delay={i * 100}"
      contains: "RevealOnScroll"
    - path: "src/components/landing/final-cta.tsx"
      provides: "FinalCta paper-card wrapped in single RevealOnScroll"
      contains: "RevealOnScroll"
  key_links:
    - from: "src/components/landing/schools-today.tsx"
      to: "src/components/landing/reveal-on-scroll.tsx"
      via: "import { RevealOnScroll } from \"./reveal-on-scroll\""
      pattern: "RevealOnScroll"
    - from: "src/components/landing/audience-columns.tsx"
      to: "src/components/landing/reveal-on-scroll.tsx"
      via: "import { RevealOnScroll } from \"./reveal-on-scroll\""
      pattern: "RevealOnScroll"
    - from: "src/components/landing/final-cta.tsx"
      to: "src/components/landing/reveal-on-scroll.tsx"
      via: "import { RevealOnScroll } from \"./reveal-on-scroll\""
      pattern: "RevealOnScroll"
    - from: "src/components/landing/reveal-on-scroll.tsx"
      to: "motion/react"
      via: "import { useInView, useReducedMotion, motion, cubicBezier } from \"motion/react\""
      pattern: "useInView|useReducedMotion"
---

<objective>
Mercury-style reveal-on-scroll polish for the three post-hero sections (`SchoolsToday`, `AudienceColumns`, `FinalCta`). Every section fades + lifts (~24px) into view once it enters the viewport, with subtle staggered children where the section has a list. The cumulative effect is what makes the page feel cohesive after the choreography ends — no single dramatic moment, just a consistent reveal rhythm.

Purpose: Close the post-choreography gap identified in the user-approved plan — once the 400lvh pinned scroll completes (~progress 0.85), the rest of the page currently pops in flat. This lands a thin, additive layer that respects the choreography (untouched), reduced-motion (final state, no animation), and the project's locked easing vocabulary.

Output:
- A reusable `<RevealOnScroll>` wrapper at `src/components/landing/reveal-on-scroll.tsx` (~40 lines)
- A vitest render test verifying the reduced-motion path and the `once: true` contract
- Three section files re-wrapped to use the component (no logic changes, no copy changes)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@/Users/rezailmi/.claude/plans/now-cehck-https-mercury-com-website-jazzy-mountain.md

<!-- The user-approved plan above is the source of truth for behavior, defaults, and section assignments. This PLAN.md operationalizes it. -->

<interfaces>
<!-- Key contracts the executor needs. Use directly — no codebase exploration required. -->

From `motion/react` (already a direct dep, see package.json:29):
```ts
// useInView — wraps IntersectionObserver, returns boolean
function useInView(
  ref: RefObject<Element>,
  options?: {
    once?: boolean
    margin?: string  // CSS-style: "0px 0px -15% 0px"
    amount?: "some" | "all" | number  // 0..1
  }
): boolean

// useReducedMotion — matches `(prefers-reduced-motion: reduce)`, returns boolean | null
function useReducedMotion(): boolean | null

// cubicBezier — easing function factory, used by motion's animate
function cubicBezier(x1: number, y1: number, x2: number, y2: number): EasingFunction

// motion.div — props.style, props.initial, props.animate, props.transition
```

Existing precedent in this repo (do NOT modify these files — read-only reference):

`src/components/landing/scroll-choreography/scroll-choreography.tsx:30-33,73-76,80`:
```tsx
import { motion, useReducedMotion, useScroll, useTransform } from "motion/react"
// ...
const prefersReducedMotion = useReducedMotion()
const isDesktop = useIsDesktop()
const reduced = prefersReducedMotion === true || !isDesktop
if (reduced) return <StaticChoreographyFallback />
```

Note: `useReducedMotion()` can return `null` during SSR/initial hydration. In the project, the established check is `=== true` (not truthy) — adopt the same pattern in `<RevealOnScroll>` so a hydration-time `null` does not erroneously skip the animation.

`src/components/landing/scroll-choreography/product-screen.tsx:51-77` — per-segment easing precedent:
```ts
import { cubicBezier, easeOut } from "motion/react"
const EASE_HERO_TO_WOW = cubicBezier(0.4, 0, 0.2, 1)
```
The reveal default ease re-uses this `cubicBezier(0.4, 0, 0.2, 1)` curve. Re-declare it locally inside `reveal-on-scroll.tsx` (do NOT extract a shared `eases.ts` — keeping this module self-contained is part of the additive-only contract).

`src/lib/utils.ts:1-7` — `cn()` helper for class composition (only needed if the wrapper accepts `className`).

Test precedent — `src/components/landing/footer.test.tsx`:
```tsx
import { describe, expect, it } from "vitest"
import { render, screen } from "@testing-library/react"
```
Use vitest + @testing-library/react. To assert the reduced-motion path, mock `motion/react`'s `useReducedMotion` in the test (vitest `vi.mock`).
</interfaces>

<!-- Target files (will be modified) -->
@src/components/landing/schools-today.tsx
@src/components/landing/audience-columns.tsx
@src/components/landing/final-cta.tsx

<!-- Read-only precedent (do NOT modify) -->
@src/lib/utils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RevealOnScroll wrapper + reduced-motion render test</name>
  <files>src/components/landing/reveal-on-scroll.tsx, src/components/landing/reveal-on-scroll.test.tsx</files>
  <behavior>
    - Default render: starts at `opacity: 0, y: 24` and animates to `opacity: 1, y: 0` once `useInView` returns true.
    - `once: true` contract: the component must use `useInView(ref, { once: true, margin: "0px 0px -15% 0px", amount: 0.25 })` so reveals do not replay on scroll-up.
    - `prefers-reduced-motion`: when `useReducedMotion()` returns `true`, render children at the final visual state (`opacity: 1`, `y: 0`) with no animation/transition. Internally short-circuit before the `useInView`-driven motion props (children must be visible to AT/keyboard from mount regardless).
    - Children visibility from mount: even in the un-revealed state, content is in the DOM and reachable to screen readers and tab order — only `opacity` + `transform` change. Do NOT add `aria-hidden`.
    - Polymorphism: accepts `as?: "div" | "section" | "article"` (default `"div"`); renders the corresponding `motion.<tag>`.
    - Stagger: accepts `delay?: number` in milliseconds (default `0`); converts to seconds for motion's `transition.delay`.
    - Defaults match the user-approved plan: `y: 24px` lift, `duration: 0.6s` (600ms), `ease: cubicBezier(0.4, 0, 0.2, 1)`, viewport `{ once: true, margin: "0px 0px -15% 0px", amount: 0.25 }`.
    - Accepts and forwards `className`, `children`, and an optional `id` (so callers can drop the wrapper directly inside layout grids without an extra `<div>`).

    Tests (vitest + @testing-library/react):
    - **Test 1 — reduced-motion final state**: mock `motion/react` such that `useReducedMotion` returns `true`; render `<RevealOnScroll><p>hello</p></RevealOnScroll>`; assert the rendered wrapper element has inline style `opacity: 1` and no `translate`/`translateY` other than 0 (or no transform at all). Children text "hello" is in the DOM.
    - **Test 2 — once-only contract**: mock `motion/react` such that `useReducedMotion` returns `false` and `useInView` is a `vi.fn()` that returns `false`; render `<RevealOnScroll>...</RevealOnScroll>`; assert `useInView` was called with an options object whose `once === true` and `margin === "0px 0px -15% 0px"`. (This is a contract test — proves the wrapper does not allow callers to silently disable the once gate.)
    - **Test 3 — children rendered from mount**: with `useInView` mocked to `false` and `useReducedMotion` to `false`, render `<RevealOnScroll><p data-testid="kid">hi</p></RevealOnScroll>`; `screen.getByTestId("kid")` resolves (children are not gated behind the in-view boolean — only their visual styles are).
  </behavior>
  <action>
    1. **Create `src/components/landing/reveal-on-scroll.tsx`** (single file, ~40-60 lines):
       - Imports: `import { motion, useInView, useReducedMotion, cubicBezier } from "motion/react"` and `import { useRef } from "react"` and `import type { ReactNode } from "react"`.
       - Local `EASE = cubicBezier(0.4, 0, 0.2, 1)` const (mirrors product-screen.tsx:51-77 precedent; do NOT export, do NOT extract to a shared `eases.ts`).
       - Constants for defaults: `DEFAULT_Y = 24`, `DEFAULT_DURATION = 0.6`, `IN_VIEW_OPTIONS = { once: true, margin: "0px 0px -15% 0px", amount: 0.25 } as const`.
       - Props type: `{ children: ReactNode; delay?: number; className?: string; id?: string; as?: "div" | "section" | "article" }`. Use `import type { ReactNode }` (verbatimModuleSyntax: true is on — see CLAUDE.md / tsconfig).
       - Body:
         ```tsx
         const ref = useRef<HTMLDivElement>(null)
         const reduced = useReducedMotion() === true  // matches scroll-choreography.tsx:76 precedent
         const inView = useInView(ref, IN_VIEW_OPTIONS)
         const Tag = motion[as] ?? motion.div  // polymorphic
         const shouldAnimate = !reduced && inView
         ```
       - Render the polymorphic `motion` element with:
         - `ref={ref}`
         - `initial={reduced ? false : { opacity: 0, y: DEFAULT_Y }}` — `initial={false}` skips the initial keyframe entirely under reduced-motion, ensuring no flash.
         - `animate={shouldAnimate || reduced ? { opacity: 1, y: 0 } : { opacity: 0, y: DEFAULT_Y }}`
         - `transition={{ duration: DEFAULT_DURATION, ease: EASE, delay: (delay ?? 0) / 1000 }}`
         - `className={className}`, `id={id}`, `children`.
       - Export named: `export function RevealOnScroll(...)`.
       - Picking the `motion` tag dynamically: prefer `const MotionTag = motion[as]` over a switch — motion exposes element accessors directly.

    2. **Create `src/components/landing/reveal-on-scroll.test.tsx`**:
       - Use the footer.test.tsx pattern (vitest + @testing-library/react).
       - Use `vi.mock("motion/react", async (importOriginal) => { ... })` to inject controllable `useInView` and `useReducedMotion` while leaving `motion`, `cubicBezier` real (or stub `motion.div` to a passthrough that forwards `style`/`children`/`data-*` if the real one is awkward in jsdom — the simplest path is usually mocking the whole module). Choose whichever keeps the test simple and reliable in jsdom.
       - Three tests as described in `<behavior>` above.
       - Run via `pnpm test` — uses vitest config inferred from package.json:9.

    3. **Constraints (re-check before commit)**:
       - Do NOT modify `scroll-choreography/**`, `paper-hero.tsx`, `feature-section.tsx`, `styles.css`, `content/landing.ts`, or `routes/index.tsx`.
       - GPU-friendly: only `transform` and `opacity` may animate.
       - No layout thrash: rely on `useInView` (IntersectionObserver), do not add scroll listeners.
       - Type-only imports use `import type` (verbatimModuleSyntax).
       - No semicolons, double quotes, 2-space indent, 80-col print width (Prettier config in CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && pnpm test -- --run reveal-on-scroll && pnpm tsc --noEmit && grep -v '^//' src/components/landing/reveal-on-scroll.tsx | grep -E "useInView|useReducedMotion|cubicBezier\\(0\\.4, 0, 0\\.2, 1\\)|once: true" | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - `src/components/landing/reveal-on-scroll.tsx` exists, exports `RevealOnScroll`, uses `useInView` with `once: true` + `margin: "0px 0px -15% 0px"`, uses `useReducedMotion()` with `=== true` check, and re-declares the `cubicBezier(0.4, 0, 0.2, 1)` ease locally.
    - `src/components/landing/reveal-on-scroll.test.tsx` exists with 3 tests covering reduced-motion final state, once-only contract, and children-rendered-from-mount.
    - `pnpm test -- --run reveal-on-scroll` passes (3/3).
    - `pnpm tsc --noEmit` passes.
    - No modifications to scroll-choreography/**, paper-hero.tsx, feature-section.tsx, styles.css, content/landing.ts, or routes/index.tsx (verified by `git status`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply RevealOnScroll to schools-today, audience-columns, final-cta</name>
  <files>src/components/landing/schools-today.tsx, src/components/landing/audience-columns.tsx, src/components/landing/final-cta.tsx</files>
  <action>
    Wrap target elements in `<RevealOnScroll>` per the user-approved plan. Make NO copy changes, NO logic changes, NO Tailwind class changes beyond moving classes to the wrapper where the wrapper replaces an existing wrapper element. Each file gets one new import line.

    1. **`src/components/landing/schools-today.tsx`** — 4 reveal points total:
       - Add `import { RevealOnScroll } from "./reveal-on-scroll"` at the top of the imports block.
       - Wrap the heading block (the inner `<div className="mx-auto max-w-2xl text-center">...</div>` containing kicker / heading / subheading) in `<RevealOnScroll>` with no delay.
       - For each `schoolsTodayCopy.cases.map((item) => ...)` iteration: wrap the `<article className="paper-card ...">` in `<RevealOnScroll delay={i * 80}>`. Update the `.map((item) => ...)` to `.map((item, i) => ...)` so the index is available. Keep the `key={item.number}` on the `RevealOnScroll` (or on the inner `<article>` — `key` belongs on the outermost mapped element, so put it on `<RevealOnScroll>`).
       - The reveal wrapper for cards should default to `as="div"` (not `as="article"`) — keep the semantic `<article>` underneath so the existing `paper-card` markup is preserved verbatim. The wrapper is a transparent layout `<div>`.
       - Do NOT change any Tailwind classes on the existing markup. Do NOT change the section-level `<section>` or its `id="schools"`.

    2. **`src/components/landing/audience-columns.tsx`** — 3 reveal points (one per column, no heading wrap per the user plan):
       - Add `import { RevealOnScroll } from "./reveal-on-scroll"`.
       - For each `audienceCopy.columns.map((column) => ...)`: change to `.map((column, i) => ...)` and wrap the existing `<article className="border-t ...">` in `<RevealOnScroll delay={i * 100}>` (note: 100ms, not 80ms — taller columns, eye needs more time per the user plan).
       - Move `key={column.label}` to the `<RevealOnScroll>`.
       - The wrapper is `as="div"` (default). The existing `<article>` stays the semantic element.

    3. **`src/components/landing/final-cta.tsx`** — 1 reveal point:
       - Add `import { RevealOnScroll } from "./reveal-on-scroll"`.
       - Wrap the inner paper-card `<div className="paper-card relative mx-auto ...">...</div>` in `<RevealOnScroll>` with no delay and no stagger (single composed unit per the user plan — "the closing beat").
       - Do NOT wrap the cloud decorations (the two `aria-hidden` decorative `<div>`s above the card) — they should remain static parallax-style decoration not subject to reveal.
       - Do NOT change the `<section id="pricing">` or its layout.

    4. **Verification of constraints**:
       - `git diff --stat` after these edits should show ONLY: `reveal-on-scroll.tsx` (new), `reveal-on-scroll.test.tsx` (new), `schools-today.tsx`, `audience-columns.tsx`, `final-cta.tsx`. Nothing else.
       - `grep -r "RevealOnScroll" src/components/landing/scroll-choreography/` must return zero matches (the choreography is untouched).
       - `grep -r "import.*paper-hero\|import.*feature-section\|import.*styles\.css\|import.*content/landing" src/components/landing/reveal-on-scroll.tsx` must return zero matches (the wrapper has no leakage into locked modules).
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && grep -l "RevealOnScroll" src/components/landing/schools-today.tsx src/components/landing/audience-columns.tsx src/components/landing/final-cta.tsx | wc -l | awk '$1 == 3 {exit 0} {exit 1}' && [ "$(grep -r 'RevealOnScroll' src/components/landing/scroll-choreography/ 2>/dev/null | wc -l)" = "0" ] && pnpm tsc --noEmit && pnpm test -- --run && pnpm build</automated>
  </verify>
  <done>
    - All three section files import and use `RevealOnScroll`.
    - SchoolsToday: heading block wrapped (no delay) + 3 cards wrapped with `delay={i * 80}` and `key` lifted to wrapper.
    - AudienceColumns: 3 columns wrapped with `delay={i * 100}` and `key` lifted to wrapper.
    - FinalCta: inner paper-card wrapped with no delay; cloud decorations unwrapped.
    - `scroll-choreography/**`, `paper-hero.tsx`, `feature-section.tsx`, `styles.css`, `content/landing.ts`, `routes/index.tsx` are untouched (`git diff --name-only` excludes them).
    - `pnpm tsc --noEmit` passes.
    - `pnpm test` passes (full suite, no regressions in any other component test).
    - `pnpm build` succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A reusable `<RevealOnScroll>` wrapper (~40-60 LOC) using `motion/react`'s `useInView` + `useReducedMotion`, applied to 7 reveal points across `SchoolsToday` (heading + 3 staggered cards), `AudienceColumns` (3 staggered columns), and `FinalCta` (single card). No edits to `scroll-choreography/**`, `paper-hero.tsx`, `feature-section.tsx`, `styles.css`, `content/landing.ts`, or `routes/index.tsx`.

    Defaults shipped: `y: 24px` lift, `duration: 600ms`, `ease: cubicBezier(0.4, 0, 0.2, 1)`, `useInView({ once: true, margin: "0px 0px -15% 0px", amount: 0.25 })`. Stagger: 80ms for SchoolsToday cards, 100ms for AudienceColumns columns. Reduced-motion → final state, no animation. Render test for the reduced-motion path + once-only contract committed.
  </what-built>
  <how-to-verify>
    Per the user-approved plan's "Required: agent-browser validation pass" — manual eyeball alone is not sufficient. Run the dev server and walk through the script:

    1. **Start dev server**: `pnpm dev` (port 3000).
    2. **Desktop pass at 1440x900** — drive `agent-browser` (or use a real browser if not available):
       a. Visit `http://localhost:3000/` — screenshot baseline at scroll y=0.
       b. Scroll slowly until `#schools` (SchoolsToday) is just entering the viewport. Confirm the heading reveals first, then the 3 case-study cards cascade in left-to-right (~80ms apart). Screenshot before-and-after.
       c. Continue to `#audiences` (AudienceColumns). Confirm the 3 columns stagger ~100ms apart (slightly slower than SchoolsToday). Screenshot before-and-after.
       d. Continue to `#pricing` (FinalCta). Confirm the single paper-card reveal fires once, no inner stagger. Cloud decorations should NOT animate — they remain static parallax. Screenshot before-and-after.
       e. Scroll back up to `#schools` — confirm reveals do NOT replay (cards remain in final state). This is the `once: true` contract in action.
       f. Capture console during the full scroll — must be clean (no React warnings, no IntersectionObserver errors, no motion warnings).

    3. **Reduced-motion pass at 1440x900** — DevTools → Rendering → Emulate CSS prefers-reduced-motion: reduce → hard-reload → repeat the scroll path. Confirm: all sections render at final state immediately, zero animation observed.

    4. **Mobile static-fallback at 390x844** — confirm the page still scrolls cleanly and reveals fire correctly in the stacked mobile layout (mobile uses the static-choreography-fallback branch, but RevealOnScroll itself runs on all devices since it's outside scroll-choreography/**).

    5. **Reload mid-page** — load `http://localhost:3000/`, immediately scroll to `#audiences`, hard-reload. Sections in the initial viewport should render at final state with no animation flash; sections still below should animate when scrolled to. Validates the SSR + IntersectionObserver hand-off.

    6. **Lighthouse before/after** — confirm no LCP regression. Reveal targets are below the fold so LCP candidate (the product screen) is unaffected, but verify.

    7. **Choreography integrity sanity-check** — scroll the hero choreography from 0 to ~progress 0.85. Confirm the morph (paper card → product screen → docked) still feels identical. The reveal layer is additive only; any regression here means the constraint was violated.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 7 checks pass, or describe specific issues (e.g., "stagger feels too fast on SchoolsToday — drop to 60ms", "FinalCta reveal is too slow", "cloud decorations animated — they shouldn't").</resume-signal>
</task>

</tasks>

<verification>
**Automated (run before checkpoint):**
- `pnpm test` — full suite green; the new `reveal-on-scroll.test.tsx` adds 3 tests, no other tests should change
- `pnpm tsc --noEmit` — passes (verbatimModuleSyntax requires `import type` for type-only imports)
- `pnpm build` — production build succeeds, no new warnings
- `pnpm lint` — clean
- `git diff --name-only` — exactly 5 files: `reveal-on-scroll.tsx` (new), `reveal-on-scroll.test.tsx` (new), `schools-today.tsx`, `audience-columns.tsx`, `final-cta.tsx`. Nothing else.
- `grep -r "RevealOnScroll" src/components/landing/scroll-choreography/` — zero matches (choreography untouched)

**Manual (checkpoint task):**
- Agent-browser walkthrough at 1440x900, 390x844, with and without prefers-reduced-motion
- Reload-mid-page test for SSR + IntersectionObserver hand-off
- Choreography integrity sanity-check (the morph still feels identical)
</verification>

<success_criteria>
- All three post-hero sections fade + lift into view as the user scrolls (~85% viewport entry trigger).
- Reveals fire exactly once per element across the page lifecycle (no replay on scroll-up).
- SchoolsToday cards cascade left-to-right at 80ms stagger; AudienceColumns columns at 100ms stagger; FinalCta is a single composed reveal.
- prefers-reduced-motion → all targets render at final state, no transform/opacity transition.
- The hero scroll choreography is visually and behaviorally identical to before — zero regression in the morph.
- No file outside the 5 declared in `files_modified` is touched.
- `pnpm test`, `pnpm tsc --noEmit`, `pnpm build`, `pnpm lint` all pass.
- Lighthouse LCP unchanged vs. baseline.
</success_criteria>

<output>
After completion, create `.planning/quick/260501-a3u-mercury-style-reveal-on-scroll-for-post-/260501-a3u-SUMMARY.md` covering:
- The shipped defaults (y, duration, ease, viewport options, stagger ms per section)
- Any deviation from the user-approved plan (none expected — flag any drift explicitly)
- Test coverage delta (3 new tests in reveal-on-scroll.test.tsx)
- Confirmed locked files were not touched (scroll-choreography/**, paper-hero.tsx, feature-section.tsx, styles.css, content/landing.ts, routes/index.tsx)
- Agent-browser screenshots or notes from the human-verify checkpoint
</output>
