---
phase: 02-orchestrator-shell-backdrop-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/landing/scroll-choreography/scroll-choreography.test.tsx
  - src/components/landing/scroll-choreography/paper-backdrop.test.tsx
  - src/components/landing/scroll-choreography/product-screen.test.tsx
  - src/components/landing/scroll-choreography/header-stacking.test.tsx
  - src/components/landing/scroll-choreography/choreography-rerender-budget.test.tsx
  - src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts
  - src/components/landing/scroll-choreography/migrate-perf-04.test.ts
  - src/components/landing/scroll-choreography/stages.test.ts
autonomous: true
requirements:
  - CHOREO-01
  - CHOREO-02
  - CHOREO-06
  - CHOREO-07
  - CHOREO-08
  - MIGRATE-01
  - MIGRATE-02
  - MIGRATE-03
  - MIGRATE-04
  - PERF-04
tags:
  - phase-2
  - testing
  - vitest
  - choreography
must_haves:
  truths:
    - "Eight Wave-0 test files exist (7 new + 1 updated stages.test.ts) and `pnpm test --run` reports them all RED with descriptive failures (no `it.skip`, no `it.todo`)"
    - "Every Phase 2 phase requirement ID (CHOREO-01,02,06,07,08, MIGRATE-01,02,03,04, PERF-04) is covered by at least one fail-loudly test stub"
    - "`@typescript-eslint/parser` is added as a devDependency so Wave 2's AST tests can run"
    - "stages.test.ts asserts the retuned `byId('wow').window[1]` value documented in CONTEXT.md D-14"
    - "Test strategy honors CONTEXT.md D-21 — orchestrator mode-switch matrix (4 cases), `<ProductScreen/>` never-unmounts mount-counter, `useScroll` `layoutEffect: false` call-signature assertion, and React DevTools Profiler smoke for SC #2 deferred to checkpoint:human-verify in Plan 05"
  artifacts:
    - path: "src/components/landing/scroll-choreography/scroll-choreography.test.tsx"
      provides: "Mode-switch matrix + className + useScroll layoutEffect:false call signature stubs"
    - path: "src/components/landing/scroll-choreography/paper-backdrop.test.tsx"
      provides: "Render shape + video gate threshold cross + loadedmetadata lifecycle stubs"
    - path: "src/components/landing/scroll-choreography/product-screen.test.tsx"
      provides: "Mount-counter (CHOREO-01) + useTransform-not-useState shape stubs"
    - path: "src/components/landing/scroll-choreography/header-stacking.test.tsx"
      provides: "MIGRATE-04 header-not-descendant-of-transformed-ancestor stub"
    - path: "src/components/landing/scroll-choreography/choreography-rerender-budget.test.tsx"
      provides: "≤2 re-renders / 100 motion-value updates stub (CHOREO-06 SC#2 + MIGRATE-02)"
    - path: "src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts"
      provides: "AST gate — no inline numeric tuples in useTransform calls"
    - path: "src/components/landing/scroll-choreography/migrate-perf-04.test.ts"
      provides: "AST gate — no animated width/height/top/left/box-shadow"
    - path: "src/components/landing/scroll-choreography/stages.test.ts"
      provides: "Updated assertion locking the retuned wow.window[1] value"
    - path: "package.json"
      provides: "Adds @typescript-eslint/parser devDependency"
  key_links:
    - from: "Wave-0 tests"
      to: "Wave 1+ source files (paper-backdrop.tsx, product-screen.tsx, scroll-choreography.tsx)"
      via: "test imports referencing not-yet-existent modules; failing imports are the fail-loudly signal"
      pattern: "import.*from \"./(paper-backdrop|product-screen|scroll-choreography)\""
---

<objective>
Land the Phase 2 Wave-0 test infrastructure: 7 new fail-loudly test stubs plus one updated `stages.test.ts`, plus the single new devDependency (`@typescript-eslint/parser`) that the two AST static-analysis tests require. No source file under test exists yet beyond the Phase 1 stubs, so every test in this plan is expected to be RED at the end of this plan; Waves 1–3 turn them GREEN.

Purpose: Lock the Phase 2 verification contract before any production code changes (per VALIDATION.md and RESEARCH.md § Validation Architecture). Phase 1 shipped 31 tests across 7 files; Phase 2 adds 7 new test files + updates one existing — the suite then enforces every CONTEXT.md D-XX decision and every Phase 2 ROADMAP success criterion.

Output:
- 7 new `*.test.ts(x)` files under `src/components/landing/scroll-choreography/`
- 1 updated test file (`stages.test.ts`) with retuned `wow.window[1]` assertion
- `package.json` + `pnpm-lock.yaml` updated to include `@typescript-eslint/parser`
- A repo-wide `pnpm test --run` that fails loudly with descriptive RED output (no `.skip`/`.todo`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-RESEARCH.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-PATTERNS.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-VALIDATION.md
@.planning/phases/01-foundation-types-static-fallback-ssr-contract/01-VERIFICATION.md
@src/components/landing/paper-hero.tsx
@src/components/landing/scroll-choreography/use-is-desktop.test.ts
@src/components/landing/scroll-choreography/static-choreography-fallback.test.tsx
@src/components/landing/landmark-audit.test.tsx
@src/components/landing/scroll-choreography/stages.test.ts
@src/components/landing/scroll-choreography/stages.ts
@src/components/landing/scroll-choreography/context.tsx
@src/components/landing/scroll-choreography/types.ts
@vitest.config.ts
@vitest.setup.ts
@package.json

<interfaces>
<!-- Key types Wave-0 tests must reference. Extracted from Phase 1 codebase. -->

From src/components/landing/scroll-choreography/stages.ts:
```typescript
export const STAGES: readonly StageDef[] // 4 entries: hero, wow, feature-a, feature-b
export function byId(id: StageId): StageDef // throws on unknown id
```

From src/components/landing/scroll-choreography/types.ts:
```typescript
export type StageId = "hero" | "wow" | "feature-a" | "feature-b"
export type StageWindow = readonly [start: number, end: number]
export type ScrollChoreographyContextValue = {
  readonly scrollYProgress: MotionValue<number>
  readonly stages: readonly StageDef[]
  readonly reducedMotion: boolean
  readonly mode: ScrollChoreographyMode  // "choreography" | "static"
}
```

From src/components/landing/scroll-choreography/context.tsx:
```typescript
export const ScrollChoreographyContext: React.Context<ScrollChoreographyContextValue>
export function useScrollChoreography(): ScrollChoreographyContextValue
```

From motion/react (used by Wave-0 tests):
```typescript
export function motionValue<T>(initial: T): MotionValue<T>
// Tests construct mock motion values via `motionValue(0)` and mutate via `.set(p)`
```

From @testing-library/react (Phase 1 convention — already in node_modules):
```typescript
export function render(ui: ReactElement): RenderResult
export function renderHook<T>(callback: () => T): RenderHookResult<T>
export function screen: typeof globalQueries
export function within(element: HTMLElement): ScopedQueries
export function waitFor(callback: () => void | Promise<void>): Promise<void>
// afterEach(cleanup) is wired globally in vitest.setup.ts
```

The vitest.setup.ts shim provides `window.matchMedia` returning `{ matches: true }` by default
(post-Phase-1 WR-04 fix uses `vi.fn`); per-test overrides follow `use-is-desktop.test.ts:20-37` pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add @typescript-eslint/parser devDependency for AST static-analysis tests</name>
  <read_first>
    - package.json (current devDependencies — verify @typescript-eslint/parser is NOT yet present)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-VALIDATION.md (line "If @typescript-eslint/parser is not yet a dependency, Wave 0 installs it")
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-RESEARCH.md § Wave 0 Gaps (AST tests use @typescript-eslint/parser)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md (no version constraint, no other parser locked)
  </read_first>
  <action>
    Run `pnpm add -D @typescript-eslint/parser@^8.0.0` from the repo root. This installs the AST parser used by `migrate-03-keyframe-binding.test.ts` and `migrate-perf-04.test.ts` (both created in Tasks 2 + 3 below). Use `^8.0.0` because eslint v9-flat-config repos commonly pair with @typescript-eslint v8 and the parser is the only piece used (no rules consumed). After install, verify `package.json` has the entry under `devDependencies` and `pnpm-lock.yaml` is updated. Do NOT add `@typescript-eslint/eslint-plugin` — only the parser is needed for AST walks. Do NOT change tsconfig, eslint.config.js, or any other config.

    Behavior: this is the only Phase 2 devDependency change. RESEARCH.md confirms motion@12.38.0 and all other libs are present.
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && grep -q '"@typescript-eslint/parser"' package.json && pnpm exec node -e "require('@typescript-eslint/parser')" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@typescript-eslint/parser"' package.json` returns >= 1
    - `pnpm-lock.yaml` contains the string `@typescript-eslint/parser`
    - `pnpm exec node -e "const p=require('@typescript-eslint/parser'); console.log(typeof p.parse)"` prints `function`
    - No other devDependency added (`git diff package.json` shows only the parser line added)
    - `pnpm test --run` still finds and runs the 7 Phase 1 test files (suite is structurally green for Phase 1, RED only for Phase 2's new files added in Tasks 2+)
  </acceptance_criteria>
  <done>devDependency added; AST parser is importable; no other config files modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 4 unit/integration test stubs (mode-switch, paper-backdrop, product-screen, header-stacking, rerender-budget)</name>
  <read_first>
    - src/components/landing/scroll-choreography/use-is-desktop.test.ts (matchMedia override pattern lines 15-38, listener-lifecycle pattern lines 40-68)
    - src/components/landing/scroll-choreography/static-choreography-fallback.test.tsx (render + role queries pattern lines 1-29)
    - src/components/landing/landmark-audit.test.tsx (HomePageFixture + within() pattern lines 15-26 and 49-53)
    - src/components/landing/paper-hero.tsx (lines 64-78 — the useState pattern Phase 2 deletes; lines 196-220 — the screen overlay → ProductScreen)
    - src/components/landing/scroll-choreography/context.tsx (defaultContextValue + ScrollChoreographyContext.Provider shape)
    - src/components/landing/scroll-choreography/stages.ts (STAGES + byId — used to look up wow.window[1])
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-PATTERNS.md § scroll-choreography.test.tsx, paper-backdrop.test.tsx, product-screen.test.tsx, header-stacking.test.tsx, choreography-rerender-budget.test.tsx (analog mappings + code snippets)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md D-02, D-15, D-16, D-21
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-VALIDATION.md § Wave 0 Requirements (the 8-bullet list — this task creates 5 of them)
    - vitest.setup.ts (matchMedia shim — already provides `{ matches: true }` default)
  </read_first>
  <action>
    Create 5 new test files that import not-yet-existent modules so they fail at import-time (this IS the fail-loudly signal). Use the `import { describe, expect, it, vi } from "vitest"` + `import { render, renderHook, screen, waitFor, within } from "@testing-library/react"` shape verbatim from Phase 1 tests. Every test body must contain real assertions (NOT `it.skip` or `it.todo`).

    **File 1 — `src/components/landing/scroll-choreography/scroll-choreography.test.tsx`:**
    Imports: `{ describe, expect, it, vi }` from "vitest"; `{ render, screen }` from "@testing-library/react"; `* as motion` from "motion/react"; `{ ScrollChoreography }` from "./scroll-choreography".
    Test 1 — `it.each` mode-switch matrix (per CONTEXT.md D-21, RESEARCH.md OQ-2): 4 cases `{isDesktop, prefersReduced, branch}` = `[{true,false,"choreography"},{true,true,"static"},{false,false,"static"},{false,true,"static"}]`. For each case: override `window.matchMedia` (use-is-desktop.test.ts:20-30 shape — return `{ matches: isDesktop, media: "(min-width: 1024px)", addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn(()=>false), onchange: null }`); spy on `useReducedMotion` via `vi.spyOn(motion, "useReducedMotion").mockReturnValue(prefersReduced ? true : false)`; render `<ScrollChoreography />`; for static branch assert `screen.getAllByRole("heading", { level: 1 }).length >= 1` AND `container.querySelector("section.scroll-choreography-only")` is null; for choreography branch assert `container.querySelector("section.scroll-choreography-only")` is non-null.
    Test 2 — `useScroll` layoutEffect:false call signature (per CONTEXT.md D-20, RESEARCH.md OQ-1): `const useScrollSpy = vi.spyOn(motion, "useScroll")`; render with desktop+motion mode (matchMedia matches:true, useReducedMotion false); assert `useScrollSpy.mock.calls.some((args) => args[0]?.layoutEffect === false)` is true. (NOTE in test docstring per RESEARCH.md OQ-1: "motion@12.38 may not recognize this option; FOUND-04 contract per Phase 1 stub. Wave 4 verifies on `pnpm preview`.")
    Test 3 — outer container className (per CONTEXT.md D-18, CHOREO-07): render with desktop+motion; assert `container.querySelector("section.scroll-choreography-only")` is non-null AND its className contains both `h-[280lvh]` and (the inner sticky child) contains `h-svh` (use querySelectorAll on `[class*='h-svh']` to find the sticky div). Also assert the outer section has NO inline `style.transform` set on it directly (header-stacking guarantee).

    **File 2 — `src/components/landing/scroll-choreography/paper-backdrop.test.tsx`:**
    Imports: `{ describe, expect, it, vi }` from "vitest"; `{ render }` from "@testing-library/react"; `{ motionValue }` from "motion/react"; `{ PaperBackdrop }` from "./paper-backdrop"; `{ ScrollChoreographyContext }` from "./context"; `{ STAGES, byId }` from "./stages".
    Add a top-of-file helper `function renderWithMockProgress(progress = 0)`: creates `const mv = motionValue(progress)`; constructs `value: ScrollChoreographyContextValue = { scrollYProgress: mv, stages: STAGES, reducedMotion: false, mode: "choreography" }`; calls `render(<ScrollChoreographyContext.Provider value={value}><PaperBackdrop /></ScrollChoreographyContext.Provider>)`; returns `{ ...utils, scrollYProgress: mv }`.
    Test 1 — MIGRATE-01 + CHOREO-02 render shape: render with progress 0; assert `container.querySelector("video")` is non-null AND has `src="/hero/teacher-working.mp4"` AND `poster="/hero/teacher-illustration.png"`; assert `container.querySelectorAll("img[src='/hero/cloud-halftone.png']").length === 2` (left + right clouds).
    Test 2 — CHOREO-08 video gate threshold cross above (per CONTEXT.md D-15/D-16): render with progress 0; obtain `const video = container.querySelector("video")!`; install `Object.defineProperty(video, "duration", { value: 5, configurable: true })` and `const pauseSpy = vi.spyOn(video, "pause")`; manually fire the loadedmetadata listener if `videoRef.current.duration` is required by the gate logic (or simulate via `video.dispatchEvent(new Event("loadedmetadata"))`); call `scrollYProgress.set(byId("wow").window[1] + 0.01)`; assert `pauseSpy` has been called at least once.
    Test 3 — CHOREO-08 video gate below threshold writes currentTime: render with progress at `byId("wow").window[1] + 0.05` initial; install duration as above; install `const setSpy = vi.spyOn(video, "currentTime", "set")`; call `scrollYProgress.set(0.3)` (well below threshold); assert `setSpy` called with a number value.
    Test 4 — loadedmetadata effect lifecycle (per CONTEXT.md D-17, paper-hero.tsx:85-95 analog): render; obtain video element; spy on `addEventListener` BEFORE render isn't possible (jsdom timing) — instead spy on `removeEventListener`, unmount, assert it was called with `"loadedmetadata"` (proves the effect attached + cleaned up).
    Test 5 — useTransform-not-useState shape (CHOREO-06): render; obtain the paper-card root via `container.querySelector(".paper-card")`; assert `(rootElement as HTMLElement).style.opacity` is set as a string OR is set by a motion value (not React state) — concretely, assert the rendered element has a non-empty inline `style` attribute with `transform` and `opacity` set (post-mount). This is a smoke test; the rerender-budget test below is the falsifiable gate.

    **File 3 — `src/components/landing/scroll-choreography/product-screen.test.tsx`:**
    Imports: same as File 2 plus `{ ProductScreen }` from "./product-screen"; reuse `renderWithMockProgress` helper inline (paste, do not import — Phase 1 conventions: no shared helpers across test files).
    Test 1 — CHOREO-01 mount-counter (NEVER unmounts): render with progress 0; locate the morphing element via `container.querySelector("[aria-label='Teacher Workspace student insights dashboard'], img[alt='Teacher Workspace student insights dashboard']")`; cache its parent `motion.div` via `const initialNode = container.querySelector("img[src='/hero/profiles-screen.png']")?.parentElement!`; call `scrollYProgress.set(0.1)` then `.set(0.3)` then `.set(0.5)` then `.set(0.7)` then `.set(0.95)`; re-query the same selector; assert the new node has the SAME object identity as `initialNode` (`expect(currentNode).toBe(initialNode)`). NOTE in test docstring: this is the Phase 2 mount-stability gate per CONTEXT.md D-21 + CHOREO-01.
    Test 2 — useTransform shape, no useState for visual props (MIGRATE-02): render; assert the morphing div has inline `style.opacity` set (motion value driven) AND inline `style.transform` containing "scale" (motion value driven).
    Test 3 — Phase 2 hero→wow ramp only (per CONTEXT.md D-09): render; assert `container.querySelector("img[src='/hero/profiles-screen.png']")` is non-null AND no element carries `data-stage="feature-a"` or `data-stage="feature-b"` (Phase 3 territory).
    Test 4 — no layoutId attribute (CHOREO-01): assert `container.querySelector("[layoutid], [data-layoutid]")` is null AND that the rendered output does NOT contain the literal string `layoutId=` (read `container.innerHTML` and assert no match).

    **File 4 — `src/components/landing/scroll-choreography/header-stacking.test.tsx`:**
    Imports: `{ describe, expect, it }` from "vitest"; `{ render, screen, within }` from "@testing-library/react"; `{ SiteFooter }` from "../footer"; `{ SiteHeader }` from "../site-header"; `{ SkipLink }` from "../skip-link"; `{ ScrollChoreography }` from "./scroll-choreography".
    Define `HomePageFixture()` verbatim from `landmark-audit.test.tsx:15-26` but with `<ScrollChoreography />` replacing `<StaticChoreographyFallback />` inside `<main id="main">`.
    Test 1 — header is NOT a descendant of any element with inline `style.transform` set (MIGRATE-04 per RESEARCH.md Pitfall 2): render `<HomePageFixture />`; `const header = screen.getByRole("banner")`; walk parent chain via `let cursor = header.parentElement; while (cursor) { expect(cursor.style.transform || "").toBe(""); cursor = cursor.parentElement }`.
    Test 2 — header is NOT inside `<main>` (D-16 sibling structure preserved through Phase 2 route swap): render; `const main = screen.getByRole("main"); expect(within(main).queryByRole("banner")).toBeNull()`.
    Test 3 — landmark count preserved post-swap: assert exactly 1 banner, 1 main, 1 contentinfo (footer). (Mirrors `landmark-audit.test.tsx:34-47`.)

    **File 5 — `src/components/landing/scroll-choreography/choreography-rerender-budget.test.tsx`:**
    Imports: `{ describe, expect, it, vi } ` from "vitest"; `{ render }` from "@testing-library/react"; `{ motionValue }` from "motion/react"; `* as motion` from "motion/react"; `{ ScrollChoreography }` from "./scroll-choreography".
    Test — CHOREO-06 / SC#2 rerender-budget gate: declare `const mv = motionValue(0)`; mock `useScroll` via `vi.spyOn(motion, "useScroll").mockReturnValue({ scrollYProgress: mv, scrollY: motionValue(0), scrollX: motionValue(0), scrollXProgress: motionValue(0) })`; declare `let renderCount = 0`; create wrapper component `function CountingChoreo(){ renderCount++; return <ScrollChoreography /> }`; override matchMedia to desktop+motion (matches:true) per use-is-desktop.test.ts:20-30; render `<CountingChoreo />`; capture `const initial = renderCount`; loop `for (let i = 0; i < 100; i++) mv.set(i / 100)`; assert `renderCount - initial <= 2` (per CONTEXT.md D-21, ROADMAP SC#2).

    File-naming and conventions checklist for all 5 files:
    - kebab-case filenames matching their target source file
    - Named imports only; no default imports; no barrel imports
    - `import type { ... }` for any type-only imports (e.g. `import type { ScrollChoreographyContextValue } from "./types"`)
    - No semicolons (Prettier config); double quotes; 2-space indent
    - Each file starts with a short docstring comment block describing what the file proves and the CONTEXT.md D-XX it implements
    - Every test must be a real `it("...", () => { ... })` with at least one `expect(...)` — NEVER `it.skip` / `it.todo`

    All 5 files MUST fail at import time with errors like `Failed to resolve import "./paper-backdrop"` until Wave 1 lands the source files. This RED state is the contract.
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && test -f src/components/landing/scroll-choreography/scroll-choreography.test.tsx && test -f src/components/landing/scroll-choreography/paper-backdrop.test.tsx && test -f src/components/landing/scroll-choreography/product-screen.test.tsx && test -f src/components/landing/scroll-choreography/header-stacking.test.tsx && test -f src/components/landing/scroll-choreography/choreography-rerender-budget.test.tsx && (! pnpm test --run 2>&1 | tee /tmp/test-w0-output.txt | grep -qE "0 failed") && grep -c "it.skip\|it.todo" src/components/landing/scroll-choreography/*.test.tsx | grep -vE ":0$" | wc -l | grep -qE "^\s*0\s*$"</automated>
  </verify>
  <acceptance_criteria>
    - Five new files exist: `scroll-choreography.test.tsx`, `paper-backdrop.test.tsx`, `product-screen.test.tsx`, `header-stacking.test.tsx`, `choreography-rerender-budget.test.tsx` under `src/components/landing/scroll-choreography/`
    - `grep -c "it.skip\\|it.todo" src/components/landing/scroll-choreography/{scroll-choreography,paper-backdrop,product-screen,header-stacking,choreography-rerender-budget}.test.tsx` returns 0 across every file
    - `pnpm test --run` exits non-zero (suite is RED) and the failing tests include each of the 5 new files (vitest output names them)
    - `grep -c "describe\\|it(" src/components/landing/scroll-choreography/scroll-choreography.test.tsx` returns >= 4 (one describe + at least 3 `it()` blocks)
    - `grep -c "describe\\|it(" src/components/landing/scroll-choreography/paper-backdrop.test.tsx` returns >= 6 (one describe + 5 `it()` blocks)
    - `grep -c "describe\\|it(" src/components/landing/scroll-choreography/product-screen.test.tsx` returns >= 5 (one describe + 4 `it()` blocks)
    - `grep -c "describe\\|it(" src/components/landing/scroll-choreography/header-stacking.test.tsx` returns >= 4 (one describe + 3 `it()` blocks)
    - `grep -c "byId(\\\"wow\\\")\\.window\\[1\\]" src/components/landing/scroll-choreography/paper-backdrop.test.tsx` returns >= 1
    - `grep -c "scroll-choreography-only" src/components/landing/scroll-choreography/scroll-choreography.test.tsx` returns >= 1
    - `grep -c "renderCount - initial" src/components/landing/scroll-choreography/choreography-rerender-budget.test.tsx` returns 1
    - `grep -c "layoutEffect" src/components/landing/scroll-choreography/scroll-choreography.test.tsx` returns >= 1
    - `grep -c "import type" src/components/landing/scroll-choreography/{scroll-choreography,paper-backdrop,product-screen,header-stacking,choreography-rerender-budget}.test.tsx | grep -v ':0$' | wc -l` >= 0 (allowed; not required)
    - All 5 files use double quotes (no single quotes) — `grep -c \"'\" file` ≤ comments-only (Prettier compliance)
  </acceptance_criteria>
  <done>Five new test files exist, contain real assertions, and fail loudly because the source modules they import don't exist yet. Phase 1's 31 tests still pass; new tests are all RED.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 2 AST static-analysis test stubs (MIGRATE-03 keyframes, PERF-04 properties) + update stages.test.ts for retuned wow.window</name>
  <read_first>
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-PATTERNS.md § migrate-03-keyframe-binding.test.ts AND § migrate-perf-04.test.ts (full code snippets and "No Analog Found" notes)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-VALIDATION.md § Wave 0 Requirements (last 3 bullets including stages.test.ts retune)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md D-12, D-13, D-14 (keyframe binding rules + retune license), D-18 (no vh)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-RESEARCH.md § Anti-Patterns (forbidden patterns) and § Critical Verification Finding (don't double-test layoutEffect here — that's scroll-choreography.test.tsx Task 2's job)
    - src/components/landing/scroll-choreography/stages.test.ts (current — verify it asserts windows in [0,1] and start<end; we are tightening one assertion)
    - src/components/landing/scroll-choreography/stages.ts (current STAGES — wow is currently `[0.2, 0.55]`; D-14 retunes to `[0.20, 0.78]`)
  </read_first>
  <action>
    Create two AST-walking test files plus update the existing stages.test.ts to lock the retuned wow.window value.

    **File 1 — `src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts`:**
    Imports: `{ describe, expect, it } ` from "vitest"; `{ readFileSync, existsSync }` from "node:fs"; `{ resolve, dirname } ` from "node:path"; `{ fileURLToPath }` from "node:url"; `{ parse } ` from "@typescript-eslint/parser".
    File header docstring: "MIGRATE-03 / D-12 / D-13: every `useTransform(motionValue, [keyframes], [outputs])` keyframe array entry MUST be either a numeric literal `0` or `1`, or a reference to `STAGES`/`byId('...').window[N]`, or a named local `const`. Forbid anonymous numeric literals like `0.6`, `0.78` in keyframe positions. Names like `HERO_COPY_LIFT_PROGRESS = 0.14` are fine because they are extracted to a const declaration."
    Constants:
    ```ts
    const __dirname_local = dirname(fileURLToPath(import.meta.url))
    const FILES = ["paper-backdrop.tsx", "product-screen.tsx", "scroll-choreography.tsx"]
    const ALLOWED_NUMERIC_LITERALS = new Set([0, 1, 0.0, 1.0])
    ```
    For each FILE: skip with `it.skipIf(!existsSync(...))` style (use `existsSync` AND `it.runIf` pattern: declare `const filePath = resolve(__dirname_local, file); const exists = existsSync(filePath)`; in the test body if `!exists` call `expect.fail("file missing — Wave 1 must create " + file)` so this WAVE-0 test stays RED until Wave 1 lands the source). Once exists: read source via `readFileSync(filePath, "utf8")`; parse with `parse(src, { loc: true, range: true, jsx: true, tokens: false })`; walk the AST top-down; for every `CallExpression` whose `callee.name === "useTransform"`, examine `args[1]` (the keyframes input array): require `args[1].type === "ArrayExpression"`; for each element of that array assert one of: `(a) Literal where value === 0 or 1`, `(b) Identifier (a named local const)`, `(c) MemberExpression matching STAGES[i].window[j] OR byId(...).window[j]` (look at the `object`/`property` chain). Reject `Literal` whose value is not in `ALLOWED_NUMERIC_LITERALS`. Use a recursive `walk(node)` function — TypeScript ESLint AST is a superset of ESTree.
    Provide one test per file via `it.each(FILES)("%s — every useTransform keyframe entry is a STAGES ref, named const, or 0/1", (file) => { ... })`.
    Note: this test will RED in Wave 0 because `paper-backdrop.tsx` and `product-screen.tsx` don't exist yet, and `scroll-choreography.tsx` exists but has no `useTransform` calls (the stub returns null). The Wave-0 RED state is intentional.

    **File 2 — `src/components/landing/scroll-choreography/migrate-perf-04.test.ts`:**
    Imports: same as File 1.
    File header docstring: "PERF-04 / RESEARCH.md Anti-Patterns: scroll-driven `motion.*` elements MUST NOT animate `width`, `height`, `top`, `left`, or `box-shadow` via motion values. Allowed style targets: scale, rotate, x, y (collectively `transform`), opacity, clipPath."
    Constants:
    ```ts
    const FILES = ["paper-backdrop.tsx", "product-screen.tsx", "scroll-choreography.tsx"]
    const FORBIDDEN_KEYS = new Set(["width", "height", "top", "left", "boxShadow", "box-shadow"])
    ```
    For each FILE: existsSync gate as above (RED if missing). Read source; parse with `@typescript-eslint/parser` (jsx:true). Walk AST; locate `JSXAttribute` nodes whose `name.name === "style"`. For each such attribute whose `value.expression` is an `ObjectExpression`, iterate properties; for each `Property` whose key matches a FORBIDDEN_KEYS entry AND whose value is NOT a `Literal` (i.e., it's an Identifier or expression — implying a motion value), flag failure with `expect.fail` listing the file + line + key. Static string literals like `style={{ width: "100%" }}` are allowed because the value is a Literal — only motion-value bindings to forbidden keys are forbidden.
    Test shape: `it.each(FILES)("%s — does not animate forbidden CSS properties via motion values", (file) => { ... })`.

    **Update — `src/components/landing/scroll-choreography/stages.test.ts`:**
    Add ONE new test at the end of the existing `describe("STAGES data", () => { ... })` block (per Phase 1 convention: one test file per source). The new test asserts the Phase-2 retuned `byId("wow").window[1]` value matches CONTEXT.md D-14 first-pass `0.78`:
    ```typescript
    it("byId('wow').window[1] is retuned to the Phase 2 first-pass value (D-14)", () => {
      // Phase 2 D-14 retunes wow.window[1] to align with the moment the screen
      // visually covers the video. First-pass: [0.20, 0.78]. Plan 04 may adjust
      // during the visual-review checkpoint; if so, update this assertion AND
      // the CONTEXT.md D-14 entry together.
      expect(byId("wow").window[1]).toBeCloseTo(0.78, 2)
    })
    ```
    Do NOT modify any existing tests in stages.test.ts. Do NOT modify stages.ts itself in this task — Wave 2's plan 04 retunes the data; this test is the contract that lock-steps with that retune.

    All three files must obey Phase 1 conventions: kebab-case, no semicolons, double quotes, 2-space indent, named imports.
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && test -f src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts && test -f src/components/landing/scroll-choreography/migrate-perf-04.test.ts && grep -q "byId(\"wow\").window\[1\]).toBeCloseTo" src/components/landing/scroll-choreography/stages.test.ts && pnpm test --run 2>&1 | tee /tmp/test-ast-output.txt | grep -qE "(failed|FAIL)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts` exists
    - File `src/components/landing/scroll-choreography/migrate-perf-04.test.ts` exists
    - `grep -c "@typescript-eslint/parser" src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts` returns >= 1
    - `grep -c "@typescript-eslint/parser" src/components/landing/scroll-choreography/migrate-perf-04.test.ts` returns >= 1
    - `grep -c "useTransform" src/components/landing/scroll-choreography/migrate-03-keyframe-binding.test.ts` returns >= 1 (asserts the AST walk targets useTransform CallExpressions)
    - `grep -c "FORBIDDEN_KEYS" src/components/landing/scroll-choreography/migrate-perf-04.test.ts` returns >= 1
    - `grep -c "width\\|height\\|top\\|left\\|box-shadow\\|boxShadow" src/components/landing/scroll-choreography/migrate-perf-04.test.ts` returns >= 5 (each FORBIDDEN key is named in the test code)
    - `grep -c "0.78\\|toBeCloseTo" src/components/landing/scroll-choreography/stages.test.ts` returns >= 1
    - `pnpm test --run` exits non-zero (full Phase 2 wave-0 suite is RED) — the failure includes `migrate-03-keyframe-binding.test.ts` and `migrate-perf-04.test.ts`
    - Phase 1's 7 test files (use-is-desktop, types, stages — except the new failing assertion, static-choreography-fallback, landmark-audit, skip-link, footer if it exists) still PASS individually (vitest output shows the Phase-1 files green by name)
    - The updated `stages.test.ts` test FAILS until Plan 04 retunes stages.ts (per D-14) — vitest output names the failing assertion explicitly with the value 0.78
  </acceptance_criteria>
  <done>Two AST-walking tests exist using @typescript-eslint/parser; stages.test.ts has the retuned-window assertion; Phase 2 wave-0 suite is RED in the expected places (8 fail-loudly stubs in total counting all 4 sets — scroll-choreography + paper-backdrop + product-screen + header-stacking + rerender-budget + migrate-03 + migrate-perf-04 + the new stages.test.ts assertion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-tool boundary (npm registry → local node_modules) | Adding `@typescript-eslint/parser` pulls a transitive dependency tree. The package is widely used and audited (typescript-eslint org), but pinning to `^8.0.0` reduces blast radius. |
| Test runtime boundary (vitest + jsdom) | Tests run in Node + jsdom; no network, no filesystem outside the repo, no production code paths exercised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `package.json` / `pnpm-lock.yaml` post-install | accept | Lockfile diff is reviewable in a single PR; @typescript-eslint/parser is a top-100 npm package. Severity LOW. |
| T-02-01-02 | Information Disclosure | AST walks read source files via `node:fs` | accept | Tests read only files inside `src/components/landing/scroll-choreography/`. No PII, no secrets, no `.env` access. Severity LOW. |
| T-02-01-03 | Denial of Service | AST parser on malformed source | accept | `@typescript-eslint/parser` returns parse errors without crashing vitest. A malformed source file would fail `pnpm test --run` clearly. Severity LOW. |
| T-02-01-04 | Repudiation | Test changes silently bypass CHOREO-06 / SC#2 contract | mitigate | Re-render-budget test is a hard `<= 2` numeric assertion. AST tests fail with explicit file+line. Phase 2 verification gate requires every Wave-0 test green before sign-off. |
</threat_model>

<verification>
After all 3 tasks land:
- `pnpm test --run` is EXPECTED to exit non-zero (Wave 0 contract: RED until Wave 1+ lands source files)
- `grep -c "it.skip\\|it.todo" src/components/landing/scroll-choreography/*.test.{ts,tsx}` returns 0 (no fake-pass stubs)
- Phase 1's existing 7 test files still pass individually (regression guard: Wave 0 must NOT break Phase 1)
- 8 Wave-0 contract files exist (5 from Task 2 + 2 from Task 3 + 1 update from Task 3): scroll-choreography.test.tsx, paper-backdrop.test.tsx, product-screen.test.tsx, header-stacking.test.tsx, choreography-rerender-budget.test.tsx, migrate-03-keyframe-binding.test.ts, migrate-perf-04.test.ts, stages.test.ts (updated)
- `package.json` devDependencies includes `@typescript-eslint/parser`
- Every requirement ID in the plan frontmatter (CHOREO-01, 02, 06, 07, 08, MIGRATE-01..04, PERF-04) maps to at least one test stub by name (cross-checked against VALIDATION.md § Wave 0 Requirements)
</verification>

<success_criteria>
- All 7 new test files exist with real assertions (no `.skip` / `.todo`)
- stages.test.ts has the D-14 retune assertion
- `@typescript-eslint/parser` is installed and importable
- `pnpm test --run` is RED — failures clearly attributable to "module not found" (paper-backdrop / product-screen / scroll-choreography body) for non-AST files, and to "expect.fail file missing" for the AST tests, and to the explicit `0.78` assertion for stages.test.ts
- Phase 1's 31 tests still report green when filtered by file
- Every Phase 2 phase-requirement ID (10 IDs) has at least one stub naming it in a docstring or `describe()` text
</success_criteria>

<output>
After completion, create `.planning/phases/02-orchestrator-shell-backdrop-migration/02-01-SUMMARY.md` recording: (a) the 7 new + 1 updated test files; (b) the new devDependency and version; (c) the RED-state evidence (vitest output excerpt naming each failing file); (d) requirement-ID-to-test-file map proving full coverage of the 10 Phase 2 requirement IDs.
</output>
