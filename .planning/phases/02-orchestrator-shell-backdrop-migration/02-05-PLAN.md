---
phase: 02-orchestrator-shell-backdrop-migration
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - src/routes/index.tsx
  - .planning/STATE.md
autonomous: false
requirements:
  - CHOREO-01
  - CHOREO-07
  - CHOREO-08
  - MIGRATE-04
# Plan 05 is the integration / verification gate — it does not introduce new
# implementation work, but it is the plan that *proves* the listed requirements
# in production. CHOREO-01 (single shared motion.div never unmounts) and CHOREO-08
# (video gate) are verified by Checkpoint B's visual smoke; CHOREO-07 (lvh/svh)
# and MIGRATE-04 (header stacking) are verified in the production preview.
# FOUND-04 is also surfaced here via Checkpoint A but FOUND-04 is a Phase 1 ID
# (carry-forward contract) — not relisted in Phase 2's requirements set.
tags:
  - phase-2
  - cutover
  - routes-swap
  - manual-verify
must_haves:
  truths:
    - "`routes/index.tsx` imports `<ScrollChoreography>` (not `<StaticChoreographyFallback>` directly) and renders it inside `<main id=\"main\" className=\"paper-page\">`"
    - "Full suite is GREEN: `pnpm test --run && pnpm typecheck && pnpm build` all exit 0"
    - "FOUND-04 / OQ-1 verdict is captured in STATE.md — either 'no flicker reproduces; motion 12.x fixed it internally' or 'flicker reproduces; escalate' (and if escalation, this plan stops here)"
    - "STAGES.wow.window first-pass `[0.20, 0.78]` is visually validated by the user OR retuned via a follow-up edit AND the new value is reflected in stages.ts AND stages.test.ts"
    - "On the desktop landing, scrolling through hero → wow shows the product-UI screenshot scaling up over the paper-card backdrop with the hero video scrubbing during stage 1 then pausing at threshold"
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "Route wires <ScrollChoreography /> into <main> (D-01 swap)"
      contains: "ScrollChoreography"
    - path: ".planning/STATE.md"
      provides: "FOUND-04 / OQ-1 verification verdict + STAGES retune outcome recorded for cross-phase reference"
      contains: "FOUND-04"
  key_links:
    - from: "routes/index.tsx <main>"
      to: "ScrollChoreography"
      via: "direct import + render"
      pattern: "<ScrollChoreography"
---

<objective>
Wire Phase 2's orchestrator into the live route, run the full automated verification suite, and execute two manual smoke checkpoints that close out CONTEXT.md OQ-1 (FOUND-04 production-build flicker) and D-14 (STAGES.wow.window retune visual review). This plan is the integration + verification gate that flips the rest of the route from the Phase 1 static fallback to the Phase 2 desktop choreography path (mobile / reduced-motion users still reach `<StaticChoreographyFallback />` via the orchestrator's internal early-return).

This plan is NON-AUTONOMOUS: it contains two `checkpoint:human-verify` gates that pause for user smoke-testing in a real browser. The user can either approve the first-pass values or request a retune of `STAGES.wow.window` (and `feature-a.window` if visual analysis warrants it — though Pitfall 7 advises Phase 3 ownership).

Purpose: Ship Phase 2 by closing the only two open verification questions — does the route actually render the choreography in production, and does the FOUND-04 contract still apply to motion@12.38. STATE.md captures the verdict so Phase 5's cutover plan inherits a clean, verified contract.

Output:
- `routes/index.tsx` swapped (D-01)
- Full automated suite confirmed green
- STATE.md updated with FOUND-04 verdict + STAGES retune outcome
- Phase 2's 5 ROADMAP success criteria fully satisfiable post-checkpoint approval
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-RESEARCH.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-VALIDATION.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-01-PLAN.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-02-PLAN.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-03-PLAN.md
@.planning/phases/02-orchestrator-shell-backdrop-migration/02-04-PLAN.md
@src/routes/index.tsx
@src/components/landing/scroll-choreography/scroll-choreography.tsx
@src/components/landing/scroll-choreography/stages.ts
@src/components/landing/scroll-choreography/static-choreography-fallback.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap routes/index.tsx to render <ScrollChoreography /> instead of <StaticChoreographyFallback /></name>
  <read_first>
    - src/routes/index.tsx (current — imports StaticChoreographyFallback at line 5; renders at line 14)
    - src/components/landing/scroll-choreography/scroll-choreography.tsx (post-Plan-04 — exports ScrollChoreography which internally branches mode and early-returns StaticChoreographyFallback for static)
    - .planning/phases/02-orchestrator-shell-backdrop-migration/02-CONTEXT.md D-01 (the locked routes-wiring decision; Phase 5 will optionally tighten the import path)
    - .planning/phases/01-foundation-types-static-fallback-ssr-contract/01-CONTEXT.md D-16 (landmark structure preserved: SkipLink + SiteHeader + main + SiteFooter as siblings)
    - src/components/landing/landmark-audit.test.tsx (uses StaticChoreographyFallback directly — DO NOT touch this file; landmark-audit's HomePageFixture intentionally tests the static composition independently of the route)
  </read_first>
  <action>
    Edit `src/routes/index.tsx`. Replace the `StaticChoreographyFallback` import + render with `ScrollChoreography`:

    Before (lines 1-19):
    ```typescript
    import { createFileRoute } from "@tanstack/react-router"

    import { SiteFooter } from "@/components/landing/footer"
    import { SiteHeader } from "@/components/landing/site-header"
    import { StaticChoreographyFallback } from "@/components/landing/scroll-choreography/static-choreography-fallback"

    export const Route = createFileRoute("/")({ component: HomePage })

    function HomePage() {
      return (
        <>
          <SiteHeader />
          <main id="main" className="paper-page">
            <StaticChoreographyFallback />
          </main>
          <SiteFooter />
        </>
      )
    }
    ```

    After:
    ```typescript
    import { createFileRoute } from "@tanstack/react-router"

    import { SiteFooter } from "@/components/landing/footer"
    import { SiteHeader } from "@/components/landing/site-header"
    import { ScrollChoreography } from "@/components/landing/scroll-choreography/scroll-choreography"

    export const Route = createFileRoute("/")({ component: HomePage })

    function HomePage() {
      return (
        <>
          <SiteHeader />
          <main id="main" className="paper-page">
            <ScrollChoreography />
          </main>
          <SiteFooter />
        </>
      )
    }
    ```

    Notes:
    - This is a 2-line edit (the import line + the JSX render line) — nothing else changes.
    - The `<StaticChoreographyFallback>` import is removed because routes/index.tsx no longer references it directly. ScrollChoreography internally early-returns `<StaticChoreographyFallback />` when `mode === "static"` (Plan 04's implementation), so static-mode users still reach the same tree.
    - `<SkipLink />` lives in `__root.tsx` (Phase 1 D-16); not touched.
    - The `paper-page` className on `<main>` is preserved (Phase 1 styling).
    - The `id="main"` skip-link target is preserved.
    - `landmark-audit.test.tsx` continues to test StaticChoreographyFallback directly because that test exercises the static-composition contract independent of the route — it must still pass post-edit. Do NOT modify it.

    After writing:
    - Run `pnpm test --run` — full suite must pass (including `header-stacking.test.tsx` which now indirectly exercises the post-swap route via its HomePageFixture that renders ScrollChoreography). The header-stacking test's HomePageFixture composes `<><SkipLink /><SiteHeader /><main id="main"><ScrollChoreography /></main><SiteFooter /></>`, mirroring the new routes/index.tsx exactly.
    - Run `pnpm typecheck` — exits 0.
    - Run `pnpm build` — exits 0 with `built in Xs`.
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && grep -q "import { ScrollChoreography }" src/routes/index.tsx && grep -q "<ScrollChoreography />" src/routes/index.tsx && ! grep -q "StaticChoreographyFallback" src/routes/index.tsx && pnpm test --run 2>&1 | tail -5 | grep -qE "Tests.*passed" && pnpm typecheck && pnpm build 2>&1 | grep -q "built in"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { ScrollChoreography }" src/routes/index.tsx` returns 1
    - `grep -c "<ScrollChoreography />" src/routes/index.tsx` returns 1
    - `grep -c "StaticChoreographyFallback" src/routes/index.tsx` returns 0 (the direct route-level reference is gone; the orchestrator owns the static early-return now)
    - The 5-line landmark structure is preserved: `grep -c "<SiteHeader />" src/routes/index.tsx` returns 1; `grep -c "<main id=\"main\"" src/routes/index.tsx` returns 1; `grep -c "<SiteFooter />" src/routes/index.tsx` returns 1; `grep -c "paper-page" src/routes/index.tsx` returns 1
    - `pnpm test --run` exits 0 — full suite (Phase 1 + Phase 2 Wave-0) GREEN
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 (production bundle emits without warnings about ScrollChoreography)
    - `landmark-audit.test.tsx` is UNCHANGED (verified via `git diff` — it still tests StaticChoreographyFallback directly)
  </acceptance_criteria>
  <done>routes/index.tsx renders ScrollChoreography. The orchestrator branches mode internally — desktop+motion users see the choreography; mobile/reduced-motion users see the static fallback (same tree as Phase 1). Full automated suite is GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint A: FOUND-04 / OQ-1 production-build flicker smoke (resolves the layoutEffect:false unknown)</name>
  <what-built>
    Plan 04 wired `useScroll({ ..., layoutEffect: false })` per the Phase 1 FOUND-04 load-bearing comment. RESEARCH.md OQ-1 (Critical Verification Finding) showed direct source-read of motion@12.38.0's `useScroll` does NOT recognize `layoutEffect` as an option — the value spreads through `...options` harmlessly. We need to confirm whether the production-build first-paint flicker (motion#2452 + paper-hero.tsx PHASE-2-DEBT) still reproduces in motion@12.38, and lock the verdict in STATE.md so Phase 5's cutover inherits a verified contract.
  </what-built>
  <how-to-verify>
    1. From the repo root, run a clean production build:
       ```bash
       pnpm build
       ```
       Expected: exits 0 with `built in Xs` in stdout. Note the stage.

    2. Start the production preview server:
       ```bash
       pnpm preview
       ```
       It should start at `http://localhost:4173/` (vite preview default) or print the actual port.

    3. Open the URL in Chrome (or your default browser) at desktop viewport ≥ 1024px wide. Open DevTools → Performance tab. Verify the choreography path is active:
       - The `<section class="scroll-choreography-only relative h-[280lvh]">` is present in the Elements panel.
       - Scrolling slowly down the page makes the paper-card scale up and the product screenshot fade in over it.

    4. Scroll roughly 50% down the page (you should see the product screenshot at peak opacity, partially overlapping the paper-card).

    5. Press `Cmd+Shift+R` (macOS) or `Ctrl+F5` (Windows/Linux) to hard-refresh, watching VERY closely for one frame. Try this 3–5 times to be sure.

       **What to look for:**
       - **Pass:** the page reloads at the mid-scroll position and shows the same visual state (or scrolls to top without a jolt). No visible flash of "stage-1-final" (paper-card fully scaled up + screen at 0 opacity) before settling.
       - **Fail (flicker reproduces):** for one frame after the refresh, you see a "wrong" stage-1-final state, then it snaps to the correct mid-scroll state.

    6. Optional rigor: in DevTools Performance tab, click the record button before the hard-refresh, capture 2–3 seconds, stop, then scrub frame-by-frame through the FCP/LCP markers. Look for any `transform: scale(5.2)` or `opacity: 0` flash on the paper-card / screen overlay during the first 1–3 frames after the refresh.

    7. Stop the preview server (Ctrl+C in the terminal).

    8. Capture the verdict (one of three outcomes) and write it into `.planning/STATE.md` under "Accumulated Context > Decisions" as a new bullet:

       **Outcome A — No flicker reproduces:**
       Add to STATE.md:
       ```
       - Phase 2 FOUND-04 / OQ-1 verdict (verified YYYY-MM-DD via `pnpm build && pnpm preview`):
         No first-paint flicker reproduces with motion@12.38.0's useScroll, even with
         layoutEffect:false passed through ...options. The Phase 1 contract is preserved
         in code as documentation, but motion 12.x appears to fix the production-flicker
         bug internally (per RESEARCH.md OQ-1: useIsomorphicLayoutEffect + needsStart ref
         pattern in use-scroll.mjs:65-84). Phase 5 may consider removing the option
         in a follow-up; Phase 2 ships with the option in place to honor the load-bearing
         comment until that follow-up runs.
       ```

       **Outcome B — Flicker reproduces:**
       Add to STATE.md:
       ```
       - Phase 2 FOUND-04 / OQ-1 verdict (verified YYYY-MM-DD via `pnpm build && pnpm preview`):
         Production-build first-paint flicker reproduces on hard-refresh mid-page.
         layoutEffect:false (as currently passed) does NOT mitigate the bug in
         motion@12.38.0. PHASE BLOCKER ESCALATION: Phase 2 cannot ship cleanly until
         the FOUND-04 contract is updated. Recommended next step: research what
         replaces layoutEffect:false in motion 12.x (the new option name OR an alternate
         API) — this is a research-phase question. Phase 2's other deliverables remain
         valid; the flicker is a SC #3 partial regression that must be tracked.
       ```
       Then STOP — do NOT proceed to Checkpoint B. The user decides whether to (a) accept the partial regression and ship Phase 2 with the bug as a known issue, (b) trigger a research phase before continuing, or (c) escalate to a Phase 2.1 / hotfix.

       **Outcome C — Inconclusive (couldn't reproduce or was ambiguous):**
       Add to STATE.md:
       ```
       - Phase 2 FOUND-04 / OQ-1 verdict (verified YYYY-MM-DD via `pnpm build && pnpm preview`):
         Inconclusive — could not reliably reproduce or rule out the flicker. The
         option layoutEffect:false is preserved in scroll-choreography.tsx as the
         Phase 1 contract; Phase 5's production deploy verification (per ROADMAP SC #4
         "no production-only useScroll-returns-0 flicker") becomes the next gate.
       ```
       Proceed to Checkpoint B.

    Capture in your reply:
    - The vite preview port that came up
    - Which of A/B/C describes your observation
    - The exact STATE.md text you appended (so the executor can update the file)
  </how-to-verify>
  <resume-signal>Reply with one of: "FOUND-04: A — no flicker, proceed", "FOUND-04: B — flicker, escalate (do NOT proceed)", or "FOUND-04: C — inconclusive, proceed". Include the STATE.md text you want recorded.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint B: STAGES.wow.window retune visual review (D-14 first-pass [0.20, 0.78])</name>
  <what-built>
    Plan 04 retuned `STAGES.wow.window` from Phase 1's first-pass `[0.2, 0.55]` to D-14's first-pass `[0.20, 0.78]`. This number controls (a) when the product screen reaches near-full opacity (`SCREEN_FADE_END` in product-screen.tsx), (b) when the paper-card backdrop fades out (`STAGE_OPACITY_FADE_END` in paper-backdrop.tsx), AND (c) the CHOREO-08 video gate threshold (`VIDEO_GATE_THRESHOLD = byId("wow").window[1]`). It is the single most consequential tunable value in Phase 2 — it determines the moment the screen "covers" the video and the GPU-pressure relief kicks in.

    The first-pass `0.78` was a planning estimate (RESEARCH.md MEDIUM confidence). User visual review may want to retune it.
  </what-built>
  <how-to-verify>
    1. Restart the preview if it stopped after Checkpoint A:
       ```bash
       pnpm build && pnpm preview
       ```
       Open at the printed URL on a desktop ≥ 1024px viewport.

    2. Smooth-scroll from the very top of the page through to the bottom of the choreography (approx 280lvh worth of scroll). Watch for the following beats:

       a. **Hero (0.0 – 0.20):** paper-card sits inside the page at scale 1; cloud-halftones drift down slowly; teacher-working video scrubs forward as you scroll; the product screenshot is invisible.
       b. **Hero → Wow transition (0.20 – 0.78):** paper-card scales up dramatically; product screenshot fades in from 0 to peak opacity AND scales from 0.55 to 1.0; teacher video continues scrubbing UNTIL you cross 0.78, at which point it pauses and stays paused.
       c. **Wow stable (0.78 – ~1.0):** paper-card is faded-out / out-of-frame; product screen sits at scale 1 with full opacity over a (now-paused) video underneath. (Phase 2 ends here; Phase 3 lands the docking transforms for feature-a / feature-b.)

    3. **The threshold question — does `0.78` feel right?**

       - Too late (`>0.78`): the video keeps scrubbing while the screen is already fully visible — wasted GPU work. Symptom: you hear/see the video moving when the screenshot fully covers it. Lower the threshold (e.g. `0.72`).
       - Too early (`<0.78`): the video pauses while the screen is still mostly transparent — visible "stutter" because the paper-card's fade-out hasn't finished. Symptom: there's a moment where the video stops abruptly and the screen is barely visible. Raise the threshold (e.g. `0.82`).
       - Just right: the video pauses at the same moment the screenshot reaches full opacity AND the paper-card finishes fading — the transition feels intentional.

       Use Chrome DevTools Performance tab if you want to confirm GPU work drops at the moment the threshold crosses (the `<video>` should stop generating compositor frames; only the paper-card → screen morph continues).

    4. **Additionally check** the visual feel of the hero copy fade-out (`HERO_COPY_FADE_OUT_END = 0.14`) — does the headline + subline fade and lift before the paper-card scales up too much? If yes, the intra-stage timing constants in scroll-choreography.tsx are fine. If the copy lingers and gets crushed by the scaling card, the constants need tweaking — but that's a follow-up nit, not a blocker for this checkpoint.

    5. Stop the preview (Ctrl+C).

    6. Choose ONE outcome:

       **Outcome A — `[0.20, 0.78]` feels right:**
       - No edits to stages.ts; no edits to stages.test.ts.
       - Add a STATE.md bullet:
         ```
         - Phase 2 D-14 STAGES.wow.window retune (verified YYYY-MM-DD via `pnpm preview` visual review):
           First-pass [0.20, 0.78] approved. The video gate fires at the moment the
           product screen reaches peak opacity. No further retune needed in Phase 2.
           Phase 3 may revisit when feature-a/b docking transforms land.
         ```
       - Reply with "STAGES retune: A — [0.20, 0.78] approved".

       **Outcome B — `[0.20, 0.78]` needs adjustment:**
       - Pick a new value `X` where `0.55 < X < 1.0` (must be > wow.window[0] = 0.2 and ≤ 1.0; recommended range 0.65–0.85 to keep the cross-fade reasonable).
       - The executor will edit `src/components/landing/scroll-choreography/stages.ts` to change `wow.window` from `[0.2, 0.78]` to `[0.2, X]` AND edit `src/components/landing/scroll-choreography/stages.test.ts` to update the assertion `expect(byId("wow").window[1]).toBeCloseTo(X, 2)`.
       - Then re-run `pnpm test --run && pnpm typecheck && pnpm build` — must all pass.
       - Then re-run `pnpm preview` and verify the new value feels right (loop back to step 2). One additional retune cycle is allowed; if 2 cycles fail to find a satisfying value, the user should escalate to a STATE.md follow-up issue rather than continue cycling.
       - Add the STATE.md bullet documenting the final value:
         ```
         - Phase 2 D-14 STAGES.wow.window retune (verified YYYY-MM-DD via `pnpm preview` visual review):
           Retuned from first-pass [0.20, 0.78] to [0.20, X] after visual review. Reason:
           {brief explanation of what felt wrong}. The video-gate threshold + CHOREO-08
           contract update with the new value via byId("wow").window[1].
         ```
       - Reply with "STAGES retune: B — new value X = {your-number}, retune approved".

       **Outcome C — Cannot decide / wants Phase 3 to revisit:**
       - Keep `[0.20, 0.78]` first-pass.
       - Add STATE.md bullet:
         ```
         - Phase 2 D-14 STAGES.wow.window retune (verified YYYY-MM-DD via `pnpm preview` visual review):
           First-pass [0.20, 0.78] retained as good-enough for Phase 2 ship. Phase 3
           visual-review checkpoint (when feature-a/b docking transforms land) will
           re-evaluate against the full choreography arc rather than just hero→wow.
         ```
       - Reply with "STAGES retune: C — defer final decision to Phase 3".

    Capture in your reply:
    - Which outcome (A/B/C)
    - If B: the new value, plus a short explanation of what didn't feel right with 0.78
    - The exact STATE.md text to append
  </how-to-verify>
  <resume-signal>Reply with one of: "STAGES retune: A — [0.20, 0.78] approved", "STAGES retune: B — new value X = {number}", or "STAGES retune: C — defer to Phase 3". If B, include the new number AND your one-sentence rationale.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: (Conditional on Checkpoint B Outcome B) Apply user's STAGES.wow.window retune AND/OR (every outcome) Update STATE.md with checkpoint verdicts</name>
  <read_first>
    - .planning/STATE.md (current — verify the "Accumulated Context > Decisions" section to know where to insert)
    - src/components/landing/scroll-choreography/stages.ts (only edit if Checkpoint B Outcome was B)
    - src/components/landing/scroll-choreography/stages.test.ts (only edit if Checkpoint B Outcome was B — sync the assertion)
    - The user's resume-signal text from Checkpoint A and Checkpoint B (provides the exact STATE.md text + new threshold value if applicable)
  </read_first>
  <action>
    Branch on the checkpoint outcomes:

    **If Checkpoint A returned Outcome B (FOUND-04 flicker reproduces):** STOP. Do NOT execute this task. The plan exits at Checkpoint A. Return control to the user; the next decision is theirs (research phase, hotfix, or accept-and-ship-with-known-issue).

    **Otherwise (FOUND-04 outcomes A or C):** Continue.

    **Step 1 — If Checkpoint B returned Outcome B (retune needed):**

    Edit `src/components/landing/scroll-choreography/stages.ts`. Change ONLY the wow window's second value:
    ```typescript
    // Before (after Plan 04):
    { id: "wow", window: [0.2, 0.78] as const, screen: "centered" },
    // After (this task):
    { id: "wow", window: [0.2, X] as const, screen: "centered" },
    ```
    where `X` is the user's chosen value from the resume-signal.

    Edit `src/components/landing/scroll-choreography/stages.test.ts`. Update the D-14 assertion to match:
    ```typescript
    // Before:
    expect(byId("wow").window[1]).toBeCloseTo(0.78, 2)
    // After:
    expect(byId("wow").window[1]).toBeCloseTo(X, 2)
    ```

    Run `pnpm test --run && pnpm typecheck && pnpm build`. All exit 0.

    **If Checkpoint B returned Outcome A or C: skip Step 1 entirely (no source edits).**

    **Step 2 — Update STATE.md (always runs unless Checkpoint A was Outcome B):**

    Read `.planning/STATE.md`. Locate the `## Accumulated Context > ### Decisions` block (around line 58 of the current file). Insert two new bullets at the bottom of that bulleted list (preserving the existing 4 Phase 1 bullets):

    1. The Checkpoint A verdict text (from the user's resume-signal — Outcome A or C).
    2. The Checkpoint B verdict text (from the user's resume-signal — Outcome A, B, or C).

    Use the EXACT text the user provided in their resume-signals. Replace `YYYY-MM-DD` with today's date (2026-04-29 from currentDate).

    Update `last_activity` in the YAML frontmatter to today's date. Update `stopped_at` to `Phase 2 plan 05 verification complete`.

    Step 3 — Final verification:
    - `pnpm test --run && pnpm typecheck && pnpm build` — all exit 0
    - `git diff --stat .planning/STATE.md` — shows the file was modified
    - `git diff --stat src/components/landing/scroll-choreography/stages.ts` — shows changes ONLY if Checkpoint B Outcome was B
  </action>
  <verify>
    <automated>cd /Users/rezailmi/Developer/marketing-teacher-workspace && grep -q "FOUND-04" .planning/STATE.md && grep -q "STAGES.wow.window retune" .planning/STATE.md && pnpm test --run 2>&1 | tail -5 | grep -qE "Tests.*passed" && pnpm typecheck && pnpm build 2>&1 | grep -q "built in"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FOUND-04" .planning/STATE.md` returns >= 1 (Checkpoint A verdict recorded)
    - `grep -c "STAGES.wow.window retune\\|D-14" .planning/STATE.md` returns >= 1 (Checkpoint B verdict recorded)
    - `grep -c "2026-04-29" .planning/STATE.md` returns >= 2 (both bullets dated today; matches frontmatter `last_activity`)
    - The frontmatter `last_activity` field is updated to `2026-04-29`
    - The frontmatter `stopped_at` field is updated to a value containing `"Phase 2"` and `"05"`
    - If Checkpoint B Outcome was B (retune): `grep -c "window: \\[0\\.2, 0\\.78\\]" src/components/landing/scroll-choreography/stages.ts` returns 0 AND the new value `X` is present in stages.ts AND `expect(byId("wow").window[1]).toBeCloseTo(X, 2)` is in stages.test.ts AND `pnpm test --run -- stages.test.ts` exits 0
    - If Checkpoint B Outcome was A or C: stages.ts and stages.test.ts are UNCHANGED (`git diff src/components/landing/scroll-choreography/stages.{ts,test.ts}` empty after this task)
    - `pnpm test --run` (full suite) exits 0
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>STATE.md captures both checkpoint verdicts. If a retune was approved, stages.ts + stages.test.ts reflect the new value and the suite stays green. Phase 2 is fully shipped: routes/index.tsx renders the choreography; the FOUND-04 question is answered; the STAGES first-pass is either approved or replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route boundary (`routes/index.tsx`) | Replaces the static-fallback render with the orchestrator render. The orchestrator internally branches mode, so end-user contract is unchanged for mobile/reduced-motion paths. |
| Production preview (vite preview) | Runs the full bundled build locally over HTTP; no network/auth concerns; localhost-only. |
| STATE.md write | Project planning artifact under `.planning/`; under git; reviewed in PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Route swap accidentally breaks the SkipLink/SiteHeader/SiteFooter sibling structure | mitigate | The 2-line edit only touches the import + the JSX node inside `<main>`. SkipLink is in `__root.tsx`; SiteHeader and SiteFooter are siblings. `landmark-audit.test.tsx` and `header-stacking.test.tsx` both exercise the structural contract — any regression fails the suite. |
| T-02-05-02 | Tampering | User-driven STAGES retune introduces a value outside [0, 1] or with start > end | mitigate | `stages.test.ts` Phase-1 assertions ("each stage has [start, end] window with start < end and both in [0, 1]") catch this. The retune assertion adds `toBeCloseTo(X, 2)` — if X is invalid, the prior validation tests trip first. |
| T-02-05-03 | Repudiation | Production-build flicker silently reintroduced post-ship without a verdict in STATE.md | mitigate | Checkpoint A's STATE.md update is mandatory; this plan does not flip to Done without it. Phase 5's cutover plan reads STATE.md. |
| T-02-05-04 | Information Disclosure | Production preview leaks the full landing-page bundle | accept | The bundle is the public marketing site (deployed to Vercel today); no internal code paths or secrets are exposed. Severity LOW. |
| T-02-05-05 | Denial of Service | If a Checkpoint B retune cycle thrashes (user cycles 5+ times), the plan stalls indefinitely | mitigate | The how-to-verify text caps retune cycles at 2 and instructs the user to escalate beyond that. The plan finishes Either-Or, never indefinite. |
</threat_model>

<verification>
After all tasks complete (Checkpoint A non-blocker outcome):
- `routes/index.tsx` renders `<ScrollChoreography />` (D-01)
- `pnpm test --run && pnpm typecheck && pnpm build` all exit 0
- `.planning/STATE.md` has 2 new "Decisions" bullets capturing Checkpoint A + Checkpoint B verdicts; frontmatter `last_activity` = 2026-04-29 and `stopped_at` mentions Phase 2 plan 05
- If retune was applied: stages.ts has the new wow.window[1] AND stages.test.ts asserts the same value
- The 5 ROADMAP Phase 2 success criteria are satisfiable: SC #1 (single shared product-screen + video gate verified by paper-backdrop.test.tsx + product-screen.test.tsx), SC #2 (≤ 2 re-renders verified by choreography-rerender-budget.test.tsx + zero useState in scroll-choreography subtree), SC #3 (header-stacking + Checkpoint A FOUND-04 verdict), SC #4 (lvh outer + svh inner + PERF-04 AST gate), SC #5 (MIGRATE-03 AST gate)

If Checkpoint A returned Outcome B: this plan exits with FOUND-04 unresolved. STATE.md captures the escalation. The user decides the next step.
</verification>

<success_criteria>
- routes/index.tsx renders ScrollChoreography (the only Phase 2 route edit per D-01)
- Phase 2's full automated suite is GREEN: `pnpm test --run && pnpm typecheck && pnpm build` all exit 0
- Checkpoint A returned an outcome (A, B, or C) and STATE.md records the verdict
- Checkpoint B returned an outcome (A, B, or C) and STATE.md records the verdict
- If Checkpoint B was B (retune): stages.ts + stages.test.ts both reflect the new value and the suite stays green
- The user understands Phase 2's deliverables match ROADMAP Phase 2 SCs #1–#5 (or the FOUND-04 / OQ-1 escalation pathway is recorded)
</success_criteria>

<output>
After completion, create `.planning/phases/02-orchestrator-shell-backdrop-migration/02-05-SUMMARY.md` recording: (a) the routes/index.tsx 2-line diff before/after; (b) full-suite test outcome (test count, build time); (c) Checkpoint A verdict (outcome + STATE.md text); (d) Checkpoint B verdict (outcome + final wow.window value + STATE.md text); (e) Phase 2 ROADMAP-SC traceability — for each of the 5 SCs, name the test or checkpoint that verifies it; (f) Phase 5 inheritance summary — what Phase 5's cutover plan inherits clean (route swap done) vs what it still owns (paper-hero.tsx deletion, StaticChoreographyFallback refactor away from PaperHero per D-03).
</output>
