/**
 * Phase 1 stub — Plan 05 implements the full StaticChoreographyFallback.
 * This file exists to satisfy TypeScript imports in Wave 0 test stubs
 * (static-choreography-fallback.test.tsx, landmark-audit.test.tsx) that were
 * written ahead of the implementation. Plan 05 replaces this body with the
 * real composition shell rendering:
 *   <PaperHero /> → <FeatureSection stage="feature-a" /> →
 *   <FeatureSection stage="feature-b" /> → <ProofStrip /> → <FinalCta />
 * Per D-02/D-03: thin shell for Phase 5 choreography cutover swap.
 */
export function StaticChoreographyFallback() {
  return null
}
