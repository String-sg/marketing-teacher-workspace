/**
 * Phase 1 stub — Plan 05 implements the full SkipLink.
 * This file exists to satisfy TypeScript imports in Wave 0 test stubs
 * (skip-link.test.tsx, landmark-audit.test.tsx) that were written ahead of the
 * implementation. Plan 05 replaces this body with the real skip-link markup
 * (A11Y-01 — sr-only-until-focused link targeting #main).
 */
export function SkipLink() {
  return null
}
