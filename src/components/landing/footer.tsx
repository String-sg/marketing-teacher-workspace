/**
 * Phase 1 stub — Plan 05 implements the full SiteFooter.
 * This file exists to satisfy TypeScript imports in Wave 0 test stubs
 * (footer.test.tsx, landmark-audit.test.tsx) that were written ahead of the
 * implementation. Plan 05 replaces this body with the real footer markup
 * reading from footerCopy (CONTENT-07, D-05).
 */
export function SiteFooter() {
  return null
}
